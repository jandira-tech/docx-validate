node_modules
dist
coverage
*.log
.DS_Store
.kilo
.qodo
.june
# Anchor third-party vendored snapshots to the repo root only — without the
# leading slash, the patterns would also ignore tests/fixtures/external/docx-templates/.
/docx-templates
/docx
docs/api
dotgithubtoport
package-lock.json
.idea/

# Local Word-probe raw log (machine-specific absolute paths; manifest is the committed artifact)
tests/word-probe-results.jsonl

# Claude Code transient session artifacts
.claude/
